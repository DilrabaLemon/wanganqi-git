<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.boye.dao.PaymentKeyBoxDao">
    
    <select id="getKeyBoxByPaymentId" resultType="PaymentKeyBox">
        select * from payment_key_box where payment_id = #{id} and delete_flag=0
    </select>
</mapper>