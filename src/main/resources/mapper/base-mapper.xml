<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boye.dao.BaseDao">
    <!--
        flushCache 将其设置为 true，任何时候只要语句被调用，都会导致本地缓存和二级缓存都会被清空，默认值：false。
        useCache 将其设置为 true，将会导致本条语句的结果被二级缓存，默认值：对 select 元素为 true。
        timeout 这个设置是在抛出异常之前，驱动程序等待数据库返回请求结果的秒数。默认值为 unset（依赖驱动）。
        fetchSize 这是尝试影响驱动程序每次批量返回的结果行数和这个设置值相等。默认值为 unset（依赖驱动）。
    
    <insert id="insertOperationRecord">
        insert into admin_operation_record(id,admin_id,operation_type)
        values(#{id},#{admin_id},#{operation_type})
    </insert>
    
    <insert id="insertOperationRecordByShop">
        insert into shop_operation_record(id,open_user_id,operation_type)
        values(#{id},#{open_user_id},#{operation_type})
    </insert>
    -->
</mapper>