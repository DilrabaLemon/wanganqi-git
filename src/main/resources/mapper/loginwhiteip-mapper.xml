<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.boye.dao.LoginWhiteIpDao">
    <select id="findLoginWhiteIp" resultType="LoginWhiteIp">
        select * from login_whiteip  where userId = #{userId} and usertype = #{usertype} and delete_flag = 0;
    </select>
</mapper>